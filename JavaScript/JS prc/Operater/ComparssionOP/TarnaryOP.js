let student = {
    a: 12,
    b: 40,
    c: 50,
    d: 35,
    e: 22
};

console.log("a:", (student.a >= 25) ? "Pass" : "Fail");
console.log("b:", (student.b >= 25) ? "Pass" : "Fail");
console.log("c:", (student.c >= 25) ? "Pass" : "Fail");
console.log("d:", (student.d >= 25) ? "Pass" : "Fail");
console.log("e:", (student.e >= 25) ? "Pass" : "Fail");

    // 100>10?console.log(true):console.log(false);
